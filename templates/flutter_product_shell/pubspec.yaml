name: flutter_product_shell
description: Product-shell template for AppFactory multi-product Flutter repos.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  app_factory_foundation:
    path: ../../packages/app_factory_foundation
  app_factory_growth:
    path: ../../packages/app_factory_growth

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
